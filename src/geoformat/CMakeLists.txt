FILE(GLOB SOURCE *.cpp)
FILE(GLOB HEADER *.h)


INCLUDE_DIRECTORIES($ENV{GDAL_INC} )
LINK_DIRECTORIES($ENV{GDAL_LIB})

ADD_LIBRARY(GeoFormat ${SOURCE} ${HEADER})

IF(WIN32)
	TARGET_LINK_LIBRARIES(GeoFormat gdal_i)
ELSE()
	TARGET_LINK_LIBRARIES(GeoFormat gdal)
ENDIF()